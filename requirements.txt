# scom-v GUI dependencies (the generation backend, ComfyUI + torch, is
# installed separately — provisioned on first run by the app itself).
PySide6>=6.6
websocket-client>=1.7
tomli>=2.0 ; python_version < "3.11"
